# App metadata for the discourse corpus conformance run (ci/corpus/run.sh).
#
# /Users/paulo/Work/lab/corpus/discourse.meta.txt records the RuboCop version
# and bundle-install approach used to capture ground truth, but not a pinned
# commit or clone URL (the checkout that produced it has since been deleted).
# COMMIT below is that repo's default-branch HEAD resolved via
# `git ls-remote https://github.com/discourse/discourse.git HEAD` on
# 2026-09-25, pinned here so every CI run lints the exact same source tree
# regardless of upstream commits landing later.
REPO=https://github.com/discourse/discourse.git
COMMIT=bab418487a463eede4c7d2f6415fe2eb966a3883

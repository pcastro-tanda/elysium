case value
in ADT[*head, ADT[*headhead, tail]]
                                 ^ Use space inside array brackets.
                 ^ Use space inside array brackets.
      ^ Use space inside array brackets.
end
